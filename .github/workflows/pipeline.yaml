name: tests
on: push


jobs:

  markdown-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: gem install --user-install -n~/bin mdl
      - run: ~/bin/mdl -s tests/markdown_rules.rb README.md bylaws.md Resolutions/
